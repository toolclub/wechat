# ── Web 框架 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0

# ── LangChain 生态 ──
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0          # ChatOpenAI（兼容 Ollama/vLLM/OpenAI 等 OpenAI 接口）
langchain-ollama>=0.2.0          # 可选：ChatOllama 等 Ollama 专用功能（Embedding 已改用 OpenAIEmbeddings）
langchain-qdrant>=0.2.0          # QdrantVectorStore

# ── LangGraph（Agent 编排） ──
langgraph>=0.3.0

# ── MCP 支持 ──
langchain-mcp-adapters>=0.1.0

# ── 向量数据库 ──
qdrant-client>=1.9.0

# ── Semantic Cache ──
redis[hiredis]>=5.0.0            # Redis 异步客户端 + RediSearch 向量索引
numpy>=1.24.0                    # FLOAT32 向量序列化

# ── 内置工具依赖 ──
ddgs>=9.0.0                      # web_search 工具（DuckDuckGo，使用 AsyncDDGS 原生异步接口）

# ── 环境变量加密 ──
cryptography>=42.0.0             # Fernet 对称加密（.env.enc 解密）
pyyaml>=6.0.0                    # 读取 /opt/secret/chatflow-secret.yml 密钥文件
